class Solution:
    def intersection(self, nums1: List[int], nums2: List[int]) -> List[int]:
        Result = []
        set_nums1 = set(nums1)
        set_nums2 = set(nums2)


        for num in set_nums2:
            if num in set_nums1:
                Result.append(num)
        
        return Result
